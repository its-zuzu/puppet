# CTF Platform - Production Environment Configuration
# Target Load: ~400 Concurrent Users
# generated-by: Security Audit

# --- Server Basics ---
NODE_ENV=production
PORT=10000
# Allow all origins or specify frontend domain (e.g., https://ctf.example.com)
CORS_ORIGIN=*
CORS_CREDENTIALS=true

# --- Database (MongoDB) ---
# Connection limits tuned for 400 users (assuming ~2-4 server instances)
MONGODB_URI=mongodb://localhost:27017/ctfquest
MONGO_MAX_POOL_SIZE=50
MONGO_MIN_POOL_SIZE=10
MONGO_MAX_IDLE_TIME=30000
MONGO_SERVER_SELECTION_TIMEOUT=5000
MONGO_SOCKET_TIMEOUT=45000
MONGO_WAIT_QUEUE_TIMEOUT=10000

# --- Redis (Cache & Rate Limits) ---
REDIS_URL=redis://localhost:6379
# Cache Durations (300s = 5m is good balance for CTF)
REDIS_USER_CACHE_TTL=5m
REDIS_CHALLENGE_CACHE_TTL=5m
REDIS_SCOREBOARD_CACHE_TTL=30s

# --- Security & Auth ---
# CRITICAL: Change these secrets in production!
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING_MIN_32_CHARS
JWT_EXPIRE=24h
JWT_REFRESH_THRESHOLD_PERCENT=10
SESSION_SECRET=CHANGE_THIS_TO_ANOTHER_RANDOM_STRING
SESSION_MAX_AGE=24h

# --- Cryptography ---
# Rounds: 10 is standard balance for security vs CPU load
BCRYPT_ROUNDS=10

# --- Rate Limiting (Tuned for Shared IPs) ---
# Usage: Window (e.g., 15m, 1h) and Max Requests

# strictLoginLimiter: Prevents credential stuffing
# 500 attempts / 15m allows for NAT/WiFi usage by many users
LOGIN_RATE_WINDOW=15m
LOGIN_RATE_MAX=500

# generalLimiter: Global API protection (DoS)
# 5000 req / 15m allows frequent polling (scoreboard, etc) without blocking
GENERAL_RATE_WINDOW=15m
GENERAL_RATE_MAX=5000

# --- Flag Submission Rules ---
# Per-User/Challenge limits (Prevent brute forcing a single flag)
FLAG_SUBMIT_MAX_ATTEMPTS=5
FLAG_SUBMIT_WINDOW=60
FLAG_SUBMIT_COOLDOWN=30

# Global IP Submission Limit (DoS Protection on submit endpoint)
# 500 subs / 1m allows for burst traffic from shared IPs
FLAG_SUBMIT_RATE_WINDOW=1m
FLAG_SUBMIT_RATE_MAX=500

# --- File Uploads ---
# Max size in bytes (5MB = 5242880)
FILE_UPLOAD_MAX_SIZE=5242880

# --- Email (SMTP) ---
# Required for Registration/Forgot Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_specific_password
FROM_EMAIL=no-reply@ctfquest.com

# --- Analytics ---
ANALYTICS_ACTIVE_USER_DAYS=30
ANALYTICS_RECENT_ACTIVITY_DAYS=7

# --- Realtime (SSE) ---
SSE_HEARTBEAT_INTERVAL=30s

# --- Feature Flags ---
# Set to 'false' to disable specific features if needed
ENABLE_REGISTRATION=true
ENABLE_SCOREBOARD=true
